name: Link Wild Event Details
on:
  push:
    paths:
      - '.github/workflows/wild-event-link.yml'
jobs:
  patch:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
      - run: |
          python - <<'PY'
          from pathlib import Path
          p=Path('index.html')
          s=p.read_text()
          notes = [
              '<div class=note>Sorteret fra sjældnest til mindst sjælden. Listen viser den særlige Daily Adventure Incense-pool.</div>',
              '<div class=note>Sorteret fra sjældnest til mindst sjælden. Listen viser den særlige Daily Adventure Incense-Pool.</div>',
              '<div class=note>Sorteret fra sjældnest til mindst sjælden.</div>',
              '<div class=note></div>'
          ]
          for note in notes:
              s=s.replace(note,'')
          p.write_text(s)
          PY
      - run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add index.html
          git diff --cached --quiet || git commit -m 'Remove remaining empty Daily Incense box'
          git push
