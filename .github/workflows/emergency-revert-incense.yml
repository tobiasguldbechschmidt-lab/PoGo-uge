name: Emergency restore menu
on:
  push:
    paths:
      - '.github/workflows/emergency-revert-incense.yml'
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Disabled legacy Incense rollback workflow.'
